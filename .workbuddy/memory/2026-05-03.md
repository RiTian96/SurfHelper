# 2026-05-03

## VIP视频解析器 v1.6.7 - 腾讯视频播放器适配

**问题**：腾讯视频改版，播放器 DOM 结构大变，旧选择器全部失效，导致解析 iframe 无法覆盖原生视频。

**排查结果**（用 agent-browser 实地访问 v.qq.com）：
- `#mod_player` → ❌ 不存在
- `.txp_player` → ❌ 不存在
- `.txp_video_container` → ❌ 不存在（新版是 `.txp_videos_container`，多了 s）
- `.txp_player_video_wrap` → ❌ 不存在

**新版播放器 DOM 层级**：
```
#player-component._mod_thumbplayer_container_ (754x424, pos:relative)
  > .txp_videos_container (视频, z:0)
  > .thumbplayer-user-mod (交互层, z:1, pointerEvents:auto)
  > .plugin_ctrl_txp_shadow (控制条阴影, z:2, pointerEvents:auto)
  > #magicdanmaku-iframe-wrapper (弹幕iframe, z:2)
```

**修复内容**：
1. `playerContainerSelectors` — 新增 `#player-component`, `.container-player`, `#main-player`, `.txp_videos_container` 排在旧选择器前面
2. `startGuardian` 主选择器 — 同上更新
3. `startGuardian` searchList — 腾讯新版选择器排在最前
4. `pauseAllNativeVideos` 腾讯处理 — 新版选择器 + 隐藏 `.thumbplayer-user-mod` / `#magicdanmaku-iframe-wrapper`
5. `nuisanceSelectors` — 新增 `.thumbplayer-user-mod`, `#magicdanmaku-iframe-wrapper`
6. 守护进程 + clearParse 的 `allNuisanceSelectors` — 同步新增
7. 版本号 1.6.6 → 1.6.7
