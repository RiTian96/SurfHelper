 
## 2026-04-21

### FongMi APK 下载器 v2.2.0 发布
- 重写为纯手动选择模式：先选文件（1/2/3/4）→再选节点（0/1/2/3/4）→失败可换节点重试
- 彻底移除自动测速与并发下载逻辑，避免 taskkill 误杀和 timeout 兼容性问题
- 已完整下载的文件自动跳过，避免重复下载
- 修复 bat `if` 嵌套 `else` 解析 bug（用 goto 分支替代）
- 修复 `set /p` 输入残留回车符导致匹配失败的问题（改用 `choice`）
- git push 已完成
